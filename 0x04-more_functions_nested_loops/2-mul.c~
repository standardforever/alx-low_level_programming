#include "main.h"

/**
 * mul - multiple to digit
 * @a: the fist digit to be multiple
 * @b: the second digit to be multiple
 * Return: the multiple
 */
int mul(int a, int b)
{
	int c;

	C = a * b;
	return (c);
}
